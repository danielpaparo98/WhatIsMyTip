# Runtime dependencies for DigitalOcean Functions / App Platform
# ─────────────────────────────────────────────────────────────────────────────
# Generated from `pyproject.toml` via:
#
#     uv export --no-hashes --no-dev --no-editable -o requirements.txt
#
# DO Functions uses `pip install -r requirements.txt` to install deps at
# deploy time. The local project itself is NOT installed (the `packages/`
# source tree is uploaded as-is and imported via `sys.path` manipulation in
# each function's `__init__.py`).
#
# Keep this file in sync with `[project.dependencies]` in `pyproject.toml`.
# Dev-only dependencies (pytest, ruff, mypy, etc.) are intentionally excluded
# via `--no-dev`.
# ─────────────────────────────────────────────────────────────────────────────

annotated-types==0.7.0
anyio==4.13.0
asyncpg==0.31.0
beautifulsoup4==4.15.0
certifi==2026.5.20
colorama==0.4.6 ; platform_system == 'Windows'
distro==1.9.0
greenlet==3.5.1
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.17
jiter==0.15.0
lxml==6.1.1
openai==2.38.0
pydantic==2.13.4
pydantic-core==2.46.4
pydantic-settings==2.14.1
python-dotenv==1.2.2
redis==8.0.0
sniffio==1.3.1
soupsieve==2.8.4
sqlalchemy==2.0.50
tqdm==4.67.3
typing-extensions==4.15.0
typing-inspection==0.4.2
tzdata==2026.2
